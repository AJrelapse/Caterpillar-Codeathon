<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" type="text/css" href="public/styles/main.css">
</head>

<main>
<body>

    <div id="myCarousel"></div>

    <section class="et-hero-tabs">
        <div class="et-hero-tab-container">
          <a class="et-hero-tab" href="/index"><img class="logo" src="public/images/Caterpillar_logo.svg.png"></a>
          <a class="et-hero-tab" href="/index">History</a>
          <a class="et-hero-tab" href="/index">Models</a>
          <a class="et-hero-tab" href="/index">Service</a>
          <a class="et-hero-tab" href="/index">Contact Us</a>
          <span class="et-hero-tab-slider"></span>
        </div>
      </section>

      

      <div class="px-4 py-5 my-5 text-center">
        <h1 class="display-5 fw-bold text-body-emphasis">PREDICTIONS</h1>
        <div class="col-lg-6 mx-auto">
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <div class="card crd-1">
                <div class="card-image"><img class="crd-img" src="public/images/122_1512630179.jpg"></div>
                <div class="category"> ENGINE </div>
                <div class="heading">  
                  
                  <% if (locals.engine<=25) { %>
                  <h1>
                    <div class="heading">The Percentage of failure is : <%= engine %></div>
                    <div class="heading">Less vulnurable to failures.</div>
                  </h1>
                  <% } else if(locals.engine >25 && locals.engine <=70) { %>
                    <div class="heading">The Percentage of failure is : <%= engine %></div>
                    <div class="heading">Moderate vulnurable to failures.</div>
                    
                  <% } else if(locals.engine >70 && locals.engine <=100) { %>
                      <div class="heading">The Percentage of failure is : <%= engine %></div>
                      <div class="heading">Highly vulnurable to failures.</div>
                  <% } else { %>
                    <div class="heading">Not yet Calculated.</div>
                <% } %>

                </div>
            </div>

            <div class="card crd-1">
                <div class="card-image"><img class="crd-img" src="public/images/fuel-system.jpeg"></div>
                <div class="category"> FUEL </div>
                <div class="heading">  

                  <% if (locals.fuel<=25) { %>
                  <h1>
                    <div class="heading">The Percentage of failure is : <%= fuel %></div>
                    <div class="heading">Less vulnurable to failures.</div>
                  </h1>
                  <% } else if(locals.fuel >25 && locals.fuel <=70) { %>
                    <div class="heading">The Percentage of failure is : <%= fuel %></div>
                    <div class="heading">Moderate vulnurable to failures.</div>
                    
                  <% } else if(locals.fuel >70 && locals.fuel <=100) { %>
                      <div class="heading">The Percentage of failure is : <%= fuel %></div>
                      <div class="heading">Highly vulnurable to failures.</div>
                  <% } else { %>
                    <div class="heading">Not yet Calculated.</div>
                <% } %>

                </div>
            </div>

            <div class="card crd-1">
                <div class="card-image"><img class="crd-img" src="/public/images/drive-train.jpeg"></div>
                <div class="category"> DRIVE </div>
                <div class="heading"> 

                  <% if (locals.drive<=25) { %>
                  <h1>
                    <div class="heading">The Percentage of failure is : <%= drive %></div>
                    <div class="heading">Less vulnurable to failures.</div>
                  </h1>
                  <% } else if(locals.drive >25 && locals.drive <=70) { %>
                    <div class="heading">The Percentage of failure is : <%= drive%></div>
                    <div class="heading">Moderate vulnurable to failures.</div>
                    
                  <% } else if(locals.drive >70 && locals.drive <=100) { %>
                      <div class="heading">The Percentage of failure is : <%= drive %></div>
                      <div class="heading">Highly vulnurable to failures.</div>
                  <% } else { %>
                    <div class="heading">Not yet Calculated.</div>
                <% } %>

                </div>
            </div>

            <div class="card crd-1">
                <div class="card-image"><img class="crd-img" src="public/images/misc.jpeg"></div>
                <div class="category"> MISC </div>
                <div class="heading">     

                  <% if (locals.misc<=25) { %>
                  <h1>
                    <div class="heading">The Percentage of failure is : <%= misc%></div>
                    <div class="heading">Less vulnurable to failures.</div>
                  </h1>
                  <% } else if(locals.misc >25 && locals.misc<=70) { %>
                    <div class="heading">The Percentage of failure is : <%= misc %></div>
                    <div class="heading">Moderate vulnurable to failures.</div>
                    
                  <% } else if(locals.misc >70 && locals.misc <=100) { %>
                      <div class="heading">The Percentage of failure is : <%= misc %></div>
                      <div class="heading">Highly vulnurable to failures.</div>
                  <% } else { %>
                    <div class="heading">Not yet Calculated.</div>
                <% } %>

                </div>
            </div>
            </div>
          </div>
        </div>
      </div>




<%- include('partials/footer.ejs') %>